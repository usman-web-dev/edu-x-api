'use strict';

/**
 * assessment-submission service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::assessment-submission.assessment-submission');
