'use strict';

/**
 *  assessment-submission controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::assessment-submission.assessment-submission');
